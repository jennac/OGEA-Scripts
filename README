This is a script to scrape all the links from the OGEA site and test their validity. 
It will store all broken links to a csv file, which will then be emailed to some user. 

Before using:
Update a config file with the following:
sender_email
sender_pw
to_email (who will recieve the report)
url (root url)

